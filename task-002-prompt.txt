Execute TASK-002 (B-002) do ProjetoX: Setup completo do ambiente Python.

CONTEXTO:
- Repositorio: C:\Users\v2admin\Documents\Obisidian\ProjetoX
- pyproject.toml existe mas sem dependencias
- ADR-008: Python como linguagem
- ADR-013: DI manual (sem framework)
- Codigo em: src/projetox/
- Nomes em portugues para classes, modulos, pacotes
- Type hints obrigatorios

TAREFAS:
1. Criar .venv com uv (Python 3.12). Se nao tiver, baixar com: uv python install 3.12
2. Atualizar pyproject.toml com todas as dependencias:
   - Producao: typer>=0.12, rich>=13, pydantic>=2, sqlalchemy>=2, asyncpg>=0.29, redis>=5, anthropic>=0.30, openai>=1, httpx>=0.27, structlog>=24
   - Dev: ruff>=0.5, mypy>=1.10, pytest>=8, pytest-asyncio>=0.24, pytest-cov>=5, pytest-mock>=3, factory-boy>=3
3. Criar ruff.toml: linha=100, regras=E,F,I,N,W,UP,B,SIM,ARG,COM,C4,T10,RUF,PL,PT,RET,TRY,EM,C90
4. Criar mypy.ini em modo strict
5. Instalar: uv pip install -e ".[dev]"
6. Criar pasta src/projetox/compartilhado/erros/ com:
   - __init__.py exportando Resultado, ErroAplicacao, ErroDominio, ErroRegraNegocio, ErroEntidadeNaoEncontrada, ErroAplicacaoServico, ErroInfraestrutura, ErroPersistencia, ErroComunicacao, ErroConfiguracao, ErroTranscricao
   - resultado.py: classe Resultado[T,E] generica com metodos ok() e falha()
   - hierarquia.py: ErroAplicacao(base) -> ErroDominio/ErroAplicacaoServico/ErroInfraestrutura com subclasses
7. Atualizar src/projetox/__init__.py com versao="0.1.0" e docstring do projeto
8. Executar: ruff check src/ (deve passar sem erros)
9. Executar: python -c "import projetox" (deve funcionar)
10. git add . && git commit -m "feat: setup ambiente Python com dependencias, linter e estrutura de erros"

IMPORTANTE: Nao modifique .opencode/ nem .gitignore. Use sempre nomes em portugues.
